# Multi-agent notes (Grok · Claude · others)

| Agent | Entry |
|-------|--------|
| Claude Code | Read `CLAUDE.md` first |
| Grok Build | Read `docs/HANDOFF_GROK_CLAUDE.md` |
| Both | Keep handoff file updated; same git remote `ramsja/CareProof` |

Do not overwrite each other's uncommitted work without `git status`. Prefer sequential sessions on `main` or short-lived feature branches (`feat/...`) then PR/merge.
